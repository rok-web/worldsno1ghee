<section class="second-products featured-products">
  {% assign second = collections.all.products | slice: 4, 2 %}
  {% for product in second %}
    {% render 'product-card', product: product %}
  {% endfor %}
</section>
{% schema %}
{
  "name": "Second Products",
  "settings": []
}
{% endschema %}
